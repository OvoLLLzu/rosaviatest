import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.example.atplquiz',
  appName: 'Тест на ATPL',
  webDir: 'dist',
  bundledWebRuntime: false,
};

export default config;